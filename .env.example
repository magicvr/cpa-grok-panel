# Copy values into /root/.hermes/.env (or export in shell)

# CPA base origin only, e.g. https://cpa.example.com
CPA_BASE_URL=

# Management API key used as: Authorization: Bearer <key>
CPA_MANAGEMENT_KEY=
